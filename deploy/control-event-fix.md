# 控制事件重复处理修复

2026-09-09 复查真实直播测试，确认下播状态两次发送由适配层重复处理造成。SDK 将一个控制帧解析为原始 `ControlEvent` 和派生的下播、暂停或恢复事件；派生类同样满足原先的 `isinstance(event, ControlEvent)`，因此被新业务处理路径再次接收。

`live_service/manager.py` 改为精确匹配 `ControlEvent`，与旧业务 `client.on(ControlEvent)` 的事件类型订阅一致。保留既有状态码与独立控制消息顺序，不影响普通弹幕、订单标签、黑名单或 SDK 其他用户对派生事件的订阅。

新增回归通过真实 SDK 帧解析与旧订阅对照，覆盖暂停→恢复→暂停→恢复→结束、SUSPENDED 的旧 `0` 映射，以及两条独立暂停消息均应保留。3 个新增用例在修复前全部失败；修复后完整 62 项通过，Ruff 和差异格式检查通过。

本次修复针对一条控制帧在本地被重复处理。先前弹幕源 ID 重复统计仍然成立，普通弹幕去重策略保持当前已确认的业务范围；不能由本次修复推断弹幕已全部去重。

部署状态：已于北京时间 2026-09-09 16:14 更新服务器 `163.7.2.127`。更新前确认无活动订阅且代码校验值与初次部署一致；保留旧文件备份，服务器完整回归 62 项通过（1.79 秒）后重启应用，健康接口正常，服务仍启用开机启动。

验证覆盖真实 SDK 的控制帧解析路径与旧事件订阅输出，本次没有重新等待真实主播下播。此前长时间直播数据属于修复前的版本。

修复后 `live_service/manager.py` SHA-256：`0a0381c5576d937251776b37c5b6d24810a15958e43c43e640dc887b93b6dad9`。服务器记录见 [控制事件修复部署状态](reports/control-event-fix.json)。
